name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-check:
    name: Lint, Format & Type Check
    runs-on: ubuntu-latest

    strategy:
      matrix:
        project: [frontend, backend]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
          cache-dependency-path: ${{ matrix.project }}/package-lock.json  # 👈 use matrix path

      - name: Install dependencies
        run: npm ci
        working-directory: ${{ matrix.project }}

      - name: Run ESLint
        run: npm run lint
        working-directory: ${{ matrix.project }}

      - name: Run Prettier
        run: npm run format
        working-directory: ${{ matrix.project }}

      - name: Run TypeScript type check
        run: npm run typecheck
        working-directory: ${{ matrix.project }}
